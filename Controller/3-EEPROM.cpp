#include "3-EEPROM.hpp"
/*
void EEPROMhandler::loadParam(int* checks, int* limit){
  EEPROM.get(MEM_CHECKS, *checks);
  EEPROM.get(MEM_LIMIT, *limit);
  Sprintln("Parameters CHECKS and LIMIT loaded from EEPROM");
}

void EEPROMhandler::saveChecks(int checks){
  EEPROM.put(MEM_CHECKS, checks);
  Sprintln("Parameter CHECKS saved to EEPROM");
}

void EEPROMhandler::saveLimit(int limit){
  EEPROM.put(MEM_LIMIT, limit);
  Sprintln("Parameter LIMIT saved to EEPROM");
}
*/
